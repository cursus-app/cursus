---
id: ST-12.5
jira: CUR-83
title: Préférences notifications fines
epic: EP-12
status: pending
priority: high
size: S
story_points: 2
sprint: 4
tier: core
labels: [core, notifs]
depends_on: []
blocks: []
assigned_agent: null
branch: null
pr: null
started_at: null
merged_at: null
---

## Contexte business

La notification est un canal puissant mais à double tranchant : trop de bruit ou des canaux non désirés transforment le produit en pollueur d'attention et déclenchent des désabonnements (email) ou des désinstallations (push). Donner à l'utilisateur un contrôle granulaire de ce qu'il reçoit, par quel canal et quand, est un prérequis pour la confiance et la rétention, et un signal premium attendu de tout produit SaaS moderne.

Cette story est transverse aux trois canaux (in-app ST-12.1, email ST-12.2, push ST-12.4) et conditionne leur acceptation : sans préférences fines, un stagiaire reçoit potentiellement 10+ emails par semaine et un formateur 30+. C'est aussi un alignement RGPD (consentement granulaire) et une exigence de la PRD persona Premium.

## Description fonctionnelle

Écran `/settings/notifications` accessible depuis le menu profil. Liste de catégories (livrable, alerte, badge, mention, digest, capstone, système) avec, pour chacune, trois toggles : in-app, email, push. Section globale en haut : quiet hours (plage horaire avec timezone), pause générale (snooze 1h / 1j / jusqu'à demain), et fréquence du digest (off / quotidien / hebdo). Les préférences sont appliquées immédiatement par tous les canaux et survivent au logout.

## Critères d'acceptation (Gherkin)

**Scénario 1 — Désactiver email pour les badges**

```gherkin
Given un user authentifié va sur /settings/notifications
When il décoche la case "email" sur la ligne "Badges débloqués"
And un badge est débloqué pour lui
Then aucun email n'est envoyé
And la notification in-app apparaît normalement
And la préférence est persistée en DB
```

**Scénario 2 — Quiet hours respectés**

```gherkin
Given un user a configuré quiet hours 22h-07h Europe/Paris
When un événement non-critique survient à 23h
Then aucun push ni email n'est envoyé immédiatement
And la notification est mise en file pour 07h05
And à 07h05 le digest des notifications nocturnes est envoyé en une fois
```

**Scénario 3 — Override pour les notifications critiques**

```gherkin
Given un user a coupé tous les emails
When une notification de catégorie "security" est émise (ex: mot de passe changé)
Then l'email est envoyé quand même
And un libellé indique que cette catégorie est non-désactivable
```

## Cas limites à traiter

- Timezone manquante dans le profil : fallback Europe/Paris avec bannière incitant à configurer
- Conflit quiet hours + digest : le digest respecte les quiet hours et part au sortir
- User sans email vérifié : toggles email grisés avec call-to-action vers vérification
- Push non autorisé navigateur : toggle push grisé avec lien vers l'aide
- Migration utilisateurs existants : défauts raisonnables (in-app=on, email=on critiques uniquement, push=off, digest=quotidien)

## Sous-tâches techniques

- **TT-12.5.1** — Model Prisma `NotificationPreference` (userId, category, inApp, email, push, quietStart, quietEnd, tz, digestFrequency) + RLS owner-only via migration SQL
- **TT-12.5.2** — Endpoint `GET/PATCH /api/me/notification-preferences` avec Zod validation
- **TT-12.5.3** — Page `/settings/notifications` avec composant `NotificationPrefsForm` (Nuxt 4 + @nuxt/ui 4.8) + autosave debounce 500ms
- **TT-12.5.4** — Service `notificationDispatcher.shouldSend(userId, category, channel)` consulté par les workers email/push/in-app via Prisma
- **TT-12.5.5** — Liste blanche des catégories non-désactivables (security, legal) côté serveur
- **TT-12.5.6** — Job différé Inngest pour notifications mises en file pendant quiet hours

## Stack UI utilisée

- `@nuxt/ui` 4.8 : `UToggle`, `UFormField`, `USelectMenu` (timezone, digest frequency), `UInput` (type="time" pour quiet hours), `UAlert` (catégories non-désactivables)
- **Tailwind CSS 4.3**
- **Tabler Icons** (`@iconify-json/tabler`) : `tabler:bell`, `tabler:bell-off`, `tabler:moon`, `tabler:mail`, `tabler:device-mobile`
- `vee-validate` ^4 + `@vee-validate/zod` pour validation du form (quiet_start < quiet_end, timezone IANA valide)
- **Motion** : `@vueuse/motion` pour transitions douces sur l'autosave indicator
- **Storybook 10** : stories pour les états Default / DigestOff / QuietHoursActive / EmailUnverified / PushBlocked

## Dépendances

- **Bloqué par** : CUR-79 (ST-12.1 in-app), CUR-80 (ST-12.2 email) — il faut un dispatcher existant à consulter
- **Bloque** : Aucun — story de polish

## Non-goals

- Pas de préférences par cohorte ou par cursus (granularité user globale uniquement, sinon explosion combinatoire UI)
- Pas de canal SMS (hors scope MVP, coût Twilio)
- Pas d'A/B test des défauts dans cette story (sera traité en post-MVP via PostHog)

## Tests à écrire

- **Unit** : `notificationDispatcher.shouldSend` (matrice catégorie × canal × quiet hours), parseur quiet hours timezone-aware
- **Integration** : PATCH preferences → re-fetch → dispatcher respecte la nouvelle valeur ; événement security → ignore préférence
- **E2E (Playwright)** : parcours user coche/décoche/sauve, recharge la page, valeur persistée
- **A11y (axe-core)** : groupes de cases à cocher avec `<fieldset>` + `<legend>`, aria-describedby pour les catégories non-désactivables
- **Security** : un user ne peut pas patcher les préférences d'un autre (RLS), tentative de désactiver une catégorie protégée renvoie 403

## Observabilité

- **Logs** : `notification.skipped` (level=info, payload={userId, category, channel, reason: 'pref_off'|'quiet_hours'}) ; `notification.preferences.updated` (level=info)
- **Métriques** : compteur `notifications_suppressed_total{channel, reason}` ; histogram `quiet_hours_queue_lag_seconds`
- **Alertes** : si `notifications_suppressed_total{reason='pref_off'}` > 80% du total émis sur 24h, alerte (signal de sur-notification)

## Considérations sécurité

- RLS strict : un user ne lit/écrit que ses propres préférences (effective via rôle `cursus_app` Prisma)
- Validation Zod côté serveur : timezone dans la liste IANA, digest_frequency dans enum, quietStart/end sont des `HH:mm`
- Catégories `security` et `legal` non-désactivables, enforced côté serveur (pas seulement UI)

## Considérations accessibilité

- Toggles UToggle avec labels visibles et `aria-checked`
- Navigation clavier complète, focus visible WCAG 2.1 AA (contraste ≥ 3:1)
- Annonce des changements par screen reader (aria-live polite sur le statut de sauvegarde)
- Quiet hours en sélecteurs natifs `<input type="time">` pour compatibilité lecteurs d'écran

## Considérations performance

- Préférences mises en cache côté worker (TTL 60s, invalidation sur update) pour éviter une lecture DB par notification émise
- Autosave debounce 500ms côté client pour éviter le N+1 PATCH
